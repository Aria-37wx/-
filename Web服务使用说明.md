# Web 服务使用说明

> 物料管理智能体 — Web 界面启动与操作指南
> 适用版本：v0.8（含 Phase 4 智能推荐）｜ 更新日期：2026-08-23（「智能推荐」页已并入「AI 对话」）

---

## 一、启动方式

### 方式 1：Windows 双击启动（推荐）

双击项目根目录的 `启动Web服务.bat`，自动在 Anaconda py312 环境启动 Streamlit。

### 方式 2：命令行启动

```bash
cd abc
python -m streamlit run warehouse_mcp/web/app.py --server.port 8501
```

### 依赖安装（首次运行）

```bash
python -m pip install "mcp>=1.0,<2" streamlit openai
```

- `mcp`：MCP Server 框架（需 1.x，2.x 已拆分 FastMCP，不兼容本项目）
- `streamlit`：Web 界面
- `openai`：LLM 调用（可选，不装则只能用离线规则）

### 访问地址

启动成功后浏览器打开 **http://localhost:8501**。

局域网/外网访问时，终端日志会打印 Network URL / External URL，用对应地址即可。

---

## 二、页面功能一览

左侧导航共 7 个页面：

| 页面 | 功能 |
|------|------|
| 库存总览 | 按关键词/大类/标签搜索库存 |
| **AI 对话** | 自然语言操作：入库/出库/查询/归还/**项目推荐**（AI 自动判断意图） |
| 入库 | 手动入库 / 智能入库（AI 推断分类） |
| 出库 | 借出（需归还）/ 领用（永久出库），耗材可一次出多件 |
| 归还 | 按手机号查询待归还并归还（显示借出数量） |
| 记录查询 | 借还记录 / 领用记录查询（含数量，可按手机号筛选） |
| 标签管理 | 查看/编辑物料标签描述 |

---

## 三、AI 对话页项目推荐使用步骤（Phase 4）

1. 左侧导航选择「AI 对话」，直接说需求，例如：

   > 我想做一辆 WiFi 遥控的四轮小车，带超声波避障
   > 我想做一个四旋翼无人机，需要哪些物料

2. AI 自动判断这是「项目推荐」意图，等待几秒得到：
   - 顶部汇总：`✅ 可满足 X 项 ｜ ⚠️ 部分满足 X 项 ｜ ❌ 缺货 X 项`
   - 物料明细列表，缺货/不足的条目自动展开，显示：
     - 库存命中明细（编号、名称、库存、位置）
     - 缺件数量警告
     - 💡 同类替代品建议（按名称/标签相似度排序）
3. 需要领料时，在底部「批量出库」：
   - 库存充足的项已自动勾选；⚠️ 部分满足的项默认不勾选（勾选后会把该类剩余库存全部出完），需要时手动勾选
   - 每个勾选项可微调出库数量（默认 = 需求量与库存的较小值）
   - 选择出库方式：借出（需归还）/ 领用（永久出库）
   - 填写操作人手机号（11 位）
   - 点击「确认批量出库」，出库报告记入对话，库存实时扣减
4. 点「继续对话」回到聊天输入框，可以接着聊下一个需求。

---

## 四、LLM 配置

| 配置方式 | 说明 |
|----------|------|
| Web 界面 | 「AI 对话」页底部「高级设置」→ 填 API Key → 应用并保存 |
| 环境变量 | `LLM_API_KEY`、`LLM_BASE_URL`（默认 DeepSeek）、`LLM_MODEL` |
| 配置文件 | `data/llm_config.json`（由界面/代码自动读写，已 gitignore） |

- 支持 DeepSeek、通义千问等 OpenAI 兼容 API
- **未配置 Key 或调用失败时自动降级为离线规则**，页面会有提示

### 离线规则

勾选「使用离线规则」后不调用 LLM，使用内置模板/关键词规则：

- 项目推荐：内置 9 个项目模板（智能小车、温湿度监测、蓝牙遥控、机械臂、平衡车、无人机、智能家居、视觉识别、电子秤）
- 智能入库 / 意图识别：内置关键词规则（覆盖常见科创物料）

离线规则适合无网环境演示，覆盖范围有限，复杂需求请配置 LLM。

---

## 五、常见问题

| 问题 | 解决 |
|------|------|
| 打开页面报 `No module named 'mcp.server.fastmcp'` | 装了 mcp 2.x，需降级：`pip install "mcp>=1.0,<2"` |
| 提示「未配置 LLM API Key」 | 在「AI 对话」页「高级设置」配置，或设置环境变量 `LLM_API_KEY` |
| LLM 调用报 401/验证失败 | 检查 Key 是否正确（`data/llm_config.json` 里应是 `sk-` 开头的裸 key，不能是 `$env:...` 之类字符串） |
| AI 对话里推荐提示「离线规则无法识别项目类型」 | 换更明确的描述（如"智能小车"、"温湿度监测"、"机械臂"），或配置 LLM |
| 想改端口 | 启动命令加 `--server.port 8080` |
