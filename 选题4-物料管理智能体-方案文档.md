# 选题4：学生创新实践工具/物料管理智能体 — 方案文档

---

## 一、赛题原始要求

> **场景4：学生创新实践工具/物料管理智能体**
>
> 为学生自主创新活动的物料自主管理提供管理智能体，实现工具、开发板的自主借还，工具/物料的入库和出库。

### 评价标准（通用）
- 解决了学院实验实践教学与交流活动、学生自主创新活动中的实际需求。
- 运用了智能体开发技术框架，实现了智能体的 **Harness 基本架构**。
- 充分发挥 **LLM 和边缘智能**的能力。
- 利用 **MCP** 对智能体用到的信号感知、数据管理、通信等模块进行标准化封装。
- 项目开发管理运用了软件工程方法和工具。

### 关键时间节点

| 节点 | 日期 |
|------|------|
| 组队完成 | 7月24日 |
| 提交初步方案 | 7月30日 |
| 智能体运行 | 8月22日 |
| 结营展示评奖 | 结营环节 |

---

## 二、智能体架构讲解（基于老师示例代码 weather.py）

### 2.1 老师的 weather.py 在做什么

> 注：老师给的 `weather.py` 教学示例已从仓库移除（本项目已完全重写），本节保留作为 MCP 原理讲解。

老师给的 `weather.py` 是一个标准的 **MCP Server**（MCP = Model Context Protocol，模型上下文协议）。它的结构非常简洁：

```
┌──────────────────────────────────────────────┐
│                  MCP Server                    │
│                                                │
│  @mcp.tool()  →  get_alerts(state)            │
│                "获取美国某州的天气预警"          │
│                                                │
│  @mcp.tool()  →  get_forecast(lat, lon)       │
│                "获取某个坐标的天气预报"          │
│                                                │
│  底层：httpx 调用美国国家气象局 API             │
│  运行：mcp.run(transport="stdio")              │
└──────────────────────────────────────────────┘
```

核心代码就三行关键模式：

```python
from mcp.server.fastmcp import FastMCP

mcp = FastMCP("weather")         # 1. 创建一个 MCP 服务

@mcp.tool()                       # 2. 用装饰器注册一个"工具"
async def get_alerts(state: str) -> str:
    # ... 业务逻辑 ...
    return result

mcp.run(transport="stdio")        # 3. 启动服务
```

### 2.2 MCP 是什么、为什么用它

MCP 的全称是 **Model Context Protocol**，可以理解为 **"LLM 调用外部功能的标准化接口"**。

**问题背景**：LLM 本身只会"说话"，不能操作数据库、不能读文件、不能发网络请求。要想让 LLM 能干实事，需要给它"手"——也就是工具（tools）。

**MCP 的价值**：它规定了一套标准格式，让你的工具（比如"入库一个物料"、"查询库存"）可以被任何支持 MCP 的 LLM 客户端（如 Claude Desktop、Cursor、VS Code 插件等）发现和调用。

### 2.3 我们的系统怎么借鉴

我们的物料管理系统本质上就是一个 MCP Server：

```
┌─────────────────────────────────────────────────────┐
│              物料管理 MCP Server                      │
│                                                      │
│  @mcp.tool()  →  add_material(...)     入库           │
│  @mcp.tool()  →  checkout(...)         出库(借出/领用) │
│  @mcp.tool()  →  return_item(...)      归还           │
│  @mcp.tool()  →  search_materials(...) 查询           │
│  @mcp.tool()  →  get_borrow_records(...) 借还记录查询  │
│  @mcp.tool()  →  recommend_for_project(...) 智能推荐  │
│                                                      │
│  底层：SQLite 数据库（物料、用户、借还记录）            │
│  运行：mcp.run(transport="stdio")                     │
└─────────────────────────────────────────────────────┘
```

用户通过网页 ? 后端调用 MCP tools ? SQLite，或者 LLM 客户端直接调用 MCP tools。

### 2.4 "Harness 基本架构"是什么意思

赛题要求的 Harness 架构就是上述 MCP Server 的结构模式：
- **感知层**：接收用户输入（网页表单 或 LLM 自然语言）
- **推理层**：LLM 决定调用哪个 MCP tool、传什么参数
- **执行层**：MCP tool 执行业务逻辑（操作数据库）
- **反馈层**：返回结果给用户

---

## 三、传统物料管理的理解

### 核心模型

```
物料入库 → 数据库存储(属性+唯一标识) → 按属性查表 → 找到物料
```

**举例**：一块型号为 `STM32F407` 的开发板入库：
- 它是 **开发板/主控**（大类）
- 型号 **STM32F407**
- 资产编号 **DEV-2024-0001**（唯一标识码）
- 存放位置、状态等附加属性

通过「属性→唯一标识」的对照表（即数据库查询），即可定位到具体物料。

**补充**：传统物料管理系统中，**类别是由人手工填写的**，这是后续智能化的切入点。

---

## 四、智能体创新点

### 创新点 1：自动判断物料类别

**场景**：用户入库一把锤子，只需输入名称"锤子"，智能体自动推断：

```
名称：锤子
  → 类别：工具
  → 子类：手动工具
  → 是否耗材：否（可反复使用）
```

无需人工逐项填写分类信息。

**可行性**：高。LLM 天然擅长语义理解和分类。做法：
1. 预设类别体系（见下文 5.2）
2. LLM 根据物料名称推理出最合适的类别
3. 用户可确认或修正

### 创新点 2：需求驱动的物料查询与推荐

**场景**：用户说"我想做一辆电赛小车"，智能体：
1. 推理出所需物料清单（电机、轮子、主控板、电池、传感器...）
2. 对照库存数据库检查哪些有、哪些缺
3. 输出：可出库物料 + 缺料预警

**方案**：LLM 推理出通用物料类型 → 在库存中模糊搜索匹配 → 返回结果 → 用户确认。

---

## 五、物料分类体系

### 5.1 数量单位与个体追踪

**统一按件数管理**。一卷锡丝 = 1 件，一块开发板 = 1 件。

- **非耗材**：入库 quantity=10 → 自动创建 10 条独立记录，每条有唯一编号（如 `DEV-0001` ~ `DEV-0010`）。每块板可以单独借出、单独归还。
- **耗材**：入库 quantity=5 → 1 条记录，库存数为 5。借出时扣减数量即可（不需要追踪每一卷焊锡丝）。

### 5.2 分类体系（MECE 原则）

**互斥 + 穷尽**。经过多轮细化，最终确定为以下 **10 大类**，各有 2-8 个子类：

| 大类 | 代码 | 子类 | 举例 |
|------|------|------|------|
| **主控板** | MC | MCU/单片机(MCU) · SBC单板机(SBC) · FPGA/CPLD(FPG) · DSP(DSP) · 其他 | Arduino Uno、STM32F407、树莓派 |
| **传感模块** | SN | 距离/位置(DST) · 温度/湿度(THM) · 运动/姿态(IMU) · 光/颜色/图像(OPT) · 环境(ENV) · 电流/电压(CVT) · 生物/医学(BIO) · 其他 | HC-SR04、DHT11、MPU6050 |
| **执行/驱动** | AC | 直流电机(DCM) · 步进电机(STP) · 舵机/伺服(SRV) · 电机驱动板(MDR) · 继电器(RLY) · 气动/液压(PNU) · 其他 | SG90、L298N、步进电机 |
| **通信模块** | CM | 蓝牙(BLE) · WiFi(WIF) · LoRa/NB-IoT(LOR) · 射频/NFC(RFD) · 以太网/CAN/485(ETH) · 其他 | HC-05、NRF24L01 |
| **显示/交互** | DP | OLED/LCD(OLD) · LED/数码管(LED) · 按键/旋钮(BTN) · 蜂鸣器/扬声器(BZR) · 触摸屏(TCH) · 其他 | 0.96 OLED、1602 LCD |
| **电源模块** | PW | 电池(BAT) · 稳压/升降压(REG) · 充电管理(CHG) · 电源适配器(ADP) · 其他 | 18650、AMS1117 |
| **电子元件** | EC | 电阻(RES) · 电容(CAP) · 电感/磁珠(IND) · 二极管(DIO) · 三极管/MOS(BJT) · IC/运放(ICS) · 晶振(XTL) · 其他 | 电阻包、电容包 |
| **连接/结构** | CN | 导线/排线(WIR) · 接插件/排针(CON) · 面包板/洞洞板(BRD) · 紧固件(FST) · 结构件(STR) · 其他 | 杜邦线、面包板 |
| **工具** | TL | 测量仪器(MSR) · 焊接工具(SLD) · 拆装工具(TOL) · 电源/信号源(PWS) · 其他 | 万用表、电烙铁 |
| **耗材** | CS | 焊料/助焊剂(SDR) · 胶带/胶水(TAP) · 热缩管(HSL) · 清洁/防静电(CLN) · 其他 | 焊锡丝、热缩管 |

### 5.3 关键属性：是否耗材

`is_consumable` 是物料的固有属性，用于 UI 提示和默认行为，但**不强制决定出库方式**：

| 属性 | 含义 | 举例 |
|------|------|------|
| **非耗材 (0)** | 可反复使用 | 开发板、万用表、电烙铁 |
| **耗材 (1)** | 用掉就没了 | 焊锡丝、热缩管、扎带 |

> 非耗材也可能被永久领走（开发板分配给比赛队伍）。出库时由操作者选择「借出」还是「领用」，见第六节。

### 5.4 标签系统（为 Phase 3 智能检索做准备）

标签与子类分工不同：

```
子类 = "这是什么类型的物料"（固定字段，互斥）
标签 = "这个物料有什么特性"（灵活字段，非互斥，带描述）
```

**标签表结构**：`tags(name, description)` — 每个标签有名称和 LLM 可读的描述文本。

**Phase 3 检索流程**：LLM 匹配用户需求（如"WiFi遥控小车"）→ 搜标签描述命中 "WiFi"、"电机驱动" → 找到 ESP32、L298N 等标签 → 定位物料。

**预置 17 个常用标签**（ESP32、STM32F4、Arduino、L298N、HC-SR04 等），可在「标签管理」页面补充描述。入库时可选择已有标签或新增标签。

---

## 六、借还机制设计

### 6.1 核心思路

**借还和领用都是出库的子类型**，由操作者在出库时选择出库方式，而不是由物料属性死板决定：

```
出库操作（操作者选择出库方式）
  │
  ├── 「借出」→ 库存 -1，创建借出记录（记录借出人 + 时间）
  │              └── 归还时 → 库存 +1，标记已归还
  │
  └── 「领用」→ 库存 -1，记录消耗/领用（无需归还，永久出库）
```

**为什么这样设计**：
- 同样是开发板：借去上课 →「借出」；分配给比赛队伍 →「领用」
- 同样是电烙铁：日常借用 →「借出」；实验室搬迁带走 →「领用」
- 对于焊锡丝等耗材，操作时默认选「领用」（选了「借出」也没意义）

物料上的 `is_consumable` 字段仅作为 UI 提示（耗材默认选中「领用」），不强制。

### 6.2 用户身份

采用**手机号**作为用户标识（先实现这个，NFC 后续再说）：
- 学生借物料时输入手机号
- 系统记录：谁、借了什么、什么时候借、什么时候还
- 可以被查询：某学生当前借了哪些东西、某物料被谁借走了

### 6.3 关键规则

| 场景 | 规则 |
|------|------|
| 借出（非耗材） | 库存 -1，生成借出记录（状态=借出中） |
| 归还 | 库存 +1，借出记录标记为已归还 |
| 领用（耗材或非耗材） | 库存 -1，记录出库（无需归还） |
| 库存不足 | 库存为 0 时不允许任何出库 |
| 查询某人借了啥 | 按手机号查未归还的借出记录 |
| 查询某物被谁借了 | 按物料 ID 查当前未归还记录 |
| 查询出库历史 | 查 outbound_records 表（含借出和领用） |

### 6.4 简化取舍

- **暂不做归还期限和逾期提醒**，先跑通基础借还流程
- **暂不做"续借"操作**，先只做借出和归还
- 用户管理从极简开始：操作时填手机号即自动创建用户

---

## 七、实现阶段划分

```
Phase 1 ──── 传统基础系统（全手工） ? 已完成
  ├── SQLite 数据库搭建（物料表、用户表、出库记录表、借还记录表）
  ├── MCP tools：入库/出库/归还/查询（5 个工具）
  ├── 借还：借出/领用、归还、记录查询
  └── 交互：Streamlit Web 界面（5 个功能页面 + 确认弹窗）

Phase 2 ──── 智能入库（AI 辅助）—— 已完成
  ├── 用户只输入物料名称和可选描述
  ├── LLM 自动推断类别、子类、耗材、标签
  ├── 自动分配存放位置（根据大类查 locations 表）
  ├── 两步确认流程（AI 推断 → 用户确认 → 入库）
  ├── 支持离线规则模式（无需 API Key）
  └── LLM API Key 支持 Web 界面配置 + 本地持久化保存

Phase 3 ──── AI 自然语言对话 —— 已完成
  ├── 用户用自然语言描述想做的事
  ├── LLM 自动判断意图（入库/查询/出库/归还）并提取参数
  ├── 入库意图 → 分类推断 → 确认表单 → 入库
  ├── 出库意图 → 搜索库存 → 选物料+手机号 → 确认借出/领用
  ├── 查询意图 → 多维度搜索（名称+标签描述）→ 展示结果
  ├── 归还意图 → 查询待归还列表
  └── 离线规则降级方案

Phase 4 ──── 智能推荐（AI 项目物料匹配）—— 已完成
  ├── 用户用自然语言描述项目需求（如"WiFi遥控四轮小车"）
  ├── LLM 推理结构化物料清单（名称/大类/子类/数量/必需性/说明）
  ├── 三级库存匹配：大类+子类精确匹配 → 名称/标签相似度替代品 → 缺料预警
  ├── 输出：可出库清单（✅/⚠️/❌）+ 同类替代建议（💡）+ 缺料汇总
  ├── 支持批量出库（手机号 + 借出/领用，逐件复用 checkout 工具）
  ├── 内置 9 个常见项目离线模板（小车/温湿度/蓝牙遥控/机械臂/平衡车/无人机/智能家居/视觉识别/电子秤）
  └── AI 对话新增 recommend 意图，"我想做一个xxx"自动路由到项目推荐
```

---

## 八、技术选型（已确认）

| 层面 | 选型 | 理由 |
|------|------|------|
| **智能体框架** | FastMCP（老师示例同款） | 赛题要求 MCP 封装，老师教的就这个 |
| **数据库** | SQLite | 零配置，单文件，够用 |
| **后端** | Python（FastMCP + 内置 ASGI） | 和 MCP server 一套代码 |
| **前端** | Streamlit | Python 原生，和 MCP server 同语言，快速出界面 |
| **LLM** | 云端 API 优先（DeepSeek / 通义千问） | Phase 1 不需要 LLM，Phase 2 再接入 |
| **用户标识** | 手机号 | 简单可行，NFC 后续再说 |

---

## 九、数据库表设计（已实现）

### materials（物料表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT | 唯一标识码，格式 `{大类码}-{子类码}-{日期}-{序号}`（如 `AC-DCM-20260809-0001`，见决策记录 #7） |
| name | TEXT | 物料名称，如"STM32F407 开发板" |
| category | TEXT | 大类，如"开发板/主控" |
| sub_category | TEXT | 子类，如"ARM Cortex-M4" |
| model | TEXT | 型号，如"STM32F407" |
| is_consumable | INTEGER (0/1) | 是否耗材（决定入库是单体记录还是合并记录） |
| quantity | INTEGER | 当前库存数量（非耗材每件=1，耗材可>1） |
| location | TEXT | 存放位置，如"柜A-1" |
| created_at | TEXT | 入库时间 |

> **关键设计**：非耗材入库 quantity=10 时，数据库中有 10 条独立记录，编号 `AC-DCM-20260809-0001` ~ `0010`，每件 quantity=1。
> 耗材入库 quantity=5 时，只有 1 条记录，quantity=5。

### users（用户表）

| 字段 | 类型 | 说明 |
|------|------|------|
| phone | TEXT | 手机号（主键） |
| name | TEXT | 姓名（可选，目前留空） |
| created_at | TEXT | 首次使用时间 |

### outbound_records（出库记录 — 含借出和领用）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 自增 ID |
| material_id | TEXT | 关联物料 |
| user_phone | TEXT | 操作人手机号 |
| mode | TEXT | 出库方式："borrow"（借出）或 "consume"（领用） |
| quantity | INTEGER | 出库数量 |
| created_at | TEXT | 出库时间 |

### borrow_records（借还记录 — 仅 borrow 模式出库时创建）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 自增 ID |
| material_id | TEXT | 关联物料 |
| user_phone | TEXT | 借出人手机号 |
| borrowed_at | TEXT | 借出时间 |
| returned_at | TEXT | 归还时间（NULL = 未还） |
| status | TEXT | "active"（借出中）/ "returned"（已归还） |

---

## 十、MCP 工具清单

| 工具名 | 功能 | 参数 | 属于 Phase |
|--------|------|------|------------|
| `add_material` | 入库；非耗材每件独立编号，耗材合并；支持标签关联 | name, category, sub_category, model, is_consumable, quantity, location, tags | Phase 1 |
| `checkout` | 出库；mode=borrow（借出）或 consume（领用） | material_id, user_phone, mode | Phase 1 |
| `return_item` | 归还；库存+1，记录标记为 returned | borrow_record_id | Phase 1 |
| `search_materials` | 按关键词/类别/标签搜索（标签搜索匹配标签名+描述） | keyword, category, tag, only_available | Phase 1 |
| `get_borrow_records` | 查询借还记录（按手机号/状态筛选） | user_phone, material_id, only_active | Phase 1 |
| `smart_add_material` | 智能入库（仅输入名称，LLM 补全分类+标签+位置） | name, description, quantity | Phase 2 |
| `recommend_for_project` | 根据项目需求推荐物料清单并对照库存（文本输出） | description, use_fake | Phase 4 |
| `analyze_project` | 项目需求分析（结构化结果，供 Web 页面渲染和批量出库） | description, use_fake | Phase 4 |

---

## 十一、已确认的决策记录

| # | 问题 | 决定 |
|---|------|------|
| 1 | 任务拆分 | Phase 1→2→3 递进，确认一致 |
| 2 | 借还机制 | 出库时操作者选择「借出」/「领用」，非由物料属性死板决定；手机号追踪到人 |
| 3 | 物料分类 | 10 大类 + 子类（MECE 原则）；非耗材每件独立编号，耗材合并数量 |
| 4 | 智能体架构 | FastMCP 框架，从 weather.py 模板出发 |
| 5 | LLM 选型 | Phase 1 不需要 LLM；Phase 2/3 使用云端 API |
| 6 | 交互形式 | Streamlit Web 界面，6 个功能页面（含标签管理） + 确认弹窗 |
| 7 | 唯一标识格式 | `{大类码}-{子类码}-{日期}-{序号}`，如 AC-DCM-20260809-0001 |
| 8 | 入库机制 | 非耗材 quantity=N → N 条独立记录；耗材 → 1 条记录 quantity=N |
| 9 | 出库确认 | 确认弹窗确认信息，确认后执行 |
| 10 | 标签系统 | 标签(name+description) 与子类分离；标签带 LLM 可读描述；为 Phase 3 智能推荐储备 |
| 11 | 子类助记码 | 每个子类有唯一 3 字母码；ID 包含子类码，为 Phase 3 按子类前缀精确检索提供基础 |
| 12 | LLM 接入 | OpenAI 兼容 API（DeepSeek/通义千问通用）；同时内置离线规则降级方案 |
| 13 | 位置分配 | 新增 locations 表（大类→推荐位置）；智能入库时自动分配，手动入库不限制 |
| 14 | 标签扩充 | 17 → 60+ 个预置标签，覆盖大学生科创常见场景 |
| 15 | Phase 4 推荐实现 | LLM 只推理物料清单，库存匹配由代码确定性完成（三级匹配 + 相似度替代品聚合）；LLM 失败自动降级离线模板；批量出库复用 checkout；AI 对话新增 recommend 意图 |

---

> 文档版本：v0.8 | 日期：2026-08-19
